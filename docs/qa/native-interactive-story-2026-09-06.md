# Native interactive story — partial evidence, NOT complete

Candidate `a016c80467fee5b474025d34f86c8f94e674c2e7`:
CI [34058107081](https://github.com/rogerwu188/nalu-voice-studio/actions/runs/34058107081)
completed successfully (both macOS architectures, runtime and Universal).
Arm64 artifact `9996631576`, ZIP SHA-256
`60d2d0ee45340046daf8a32db84c7c49cb1abbe37a891dda59ebf792572d2ded`.
Downloaded checksum matched; deep strict code-signature verification passed.
This is an ad-hoc QA build, not a Developer ID/notarized release.

Native app installed for this QA at `/tmp/nalu-story.msvEac/app/Nalu Voice Studio.app`.
Existing isolated SQLite was backed up at `/tmp/nalu-story.msvEac/pre-upgrade.sqlite3`
before graceful quit/replacement. Launch Services started the app using the same
isolated support directory `tmp.gEDfvkaLEc` and port 18769. Health returned OK,
schema 27. Native accessibility tree showed both previous projects intact and
the updated initial-story prompt rather than the old failed-search conversation.
The app was explicitly switched to local dictation/TTS; no microphone was started.

A third `[QA] 故事恢复验证` project was created via local API with synthetic input
and an explicitly labeled synthetic episode draft (not generated by a provider).
Project ID `prj_b9c8e04c574e4984bf46270c0c79a26f`, interactive revision 2.
After another graceful restart, its row appeared in the native project list.
However, accessibility selection/click attempts did not establish that the draft
was displayed. Therefore native conversation restoration/adoption is **not passed**.
Do not confuse row visibility with working project navigation or restored content.

Open: verify row activation/draft display, actual writer invocation and narration
corrections, source selection, script adoption/provenance reconciliation, production
and final release. No paid model/video call or publication was executed in this QA.

Separate follow-up changes the initial mode to local dictation/TTS per the user;
Realtime remains optional. It has a regression test and local parse evidence but
must pass its own compiled CI; it is not included in the installed candidate above.
