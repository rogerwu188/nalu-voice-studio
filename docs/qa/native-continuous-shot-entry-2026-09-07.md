# Native continuous-shot entry — IN_PROGRESS

Selected continuous shots now display a dedicated native preparation control
instead of the new-opening-image flow. The runtime resolves the immediately prior
task's latest adoption, extracts its accepted final frame, then prepares the
selected shot with the existing validated continuation contract. The user does
not enter a review ID, tail ID or professional contract.

This local action does not submit generation. The resulting native panel reuses
the existing published-price observation and explicit cost confirmation/dispatch
controls. Reopening permits explicit idempotent preparation to recover the same
saved task. It does not automatically post on view appearance. Failure preserves
the project/videos and explains that the previous shot must be viewed/adopted.

32 backend regressions passed in 12.75s, including automatic continuation lookup,
restart replay, stale plan/index rejection and revoked previous adoption.
Native URLProtocol coverage checks the exact plan/index, credential-free request,
runtime-selected tail and rejection of wrong-shot responses. Native tests await CI.
Full-repository Ruff passes after formatting the expanded app import.

c3405fb full CI 34088470406 succeeded. Parent 646a2cd CI 34089059572 pending and
review bridge 2dec2e0 CI 34088759895 running at check. Current installed-app QA,
real provider continuation and full episode/master/release acceptance remain open.

Impeccable hardening preserves incumbent large native controls and recovery;
there is no visual redesign or voice-engine work. No SOP changes to PASS.
