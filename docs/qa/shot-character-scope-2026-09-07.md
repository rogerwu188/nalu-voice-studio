# Reviewed shot → confirmed project character scope

IN_PROGRESS: existing confirmed-catalog path implemented, new-story catalog
creation and complete real production remain unfinished.

The actual video preparation path resolves selected character designs against
the intact production package's confirmed library. Exact names or an explicitly
linked source photo may identify one character; ambiguous or missing identities
are rejected. Two designs cannot silently become one person. Multiple instances
cannot silently become one instance to satisfy the provider's present contract.

The resulting scope contains authoritative visible IDs, absent IDs/names and
catalog/package hashes. No uploaded reference index or independent visual check
is fabricated. This image-only preparation path still needs real accepted opening
frames; it does not manufacture an H3 multi-reference request. Shot planning now
receives only confirmed character names/IDs/source-asset links from that snapshot,
not private unapproved memories or the entire project bible.

Tests:

- Scope/compiler/planning: 30 passed in 11.81s before final integration updates.
- Final scope/planning: **27 passed in 16.55s**, including real approved-plan
  binding into a named visible character. Cases cover name/photo resolution,
  missing/ambiguous/unconfirmed catalog, repeated identity, multiple instances,
  malformed catalog and tampered package.
- Ruff, diff whitespace and OpenAPI checks passed.
- Full Python run started separately (execution session 91694); not yet a PASS.
- Prior native checkpoint `0a15e68` CI 34075561888: ARM, Intel and runtime passed;
  Universal queued at inspection. Not current-candidate native acceptance.

Remaining: narratively obtain and confirm missing project characters without
professional forms; explicitly rebuild/reconcile a new immutable production
snapshot when characters are confirmed after its creation, preserving existing
plans and avoiding duplicate tasks; bind reviewed real assets/frame bytes and
expose per-shot preparation in the native flow. No paid model/provider or release
calls were made here. No SOP was promoted to PASS.
